{
  "name": "jjllmuse",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": { "jjllmuse": "./src/cli.ts" },
  "scripts": {
    "build": "mkdir -p dist && bun run build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
