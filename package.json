{
  "name": "jjllmuse",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "bin": { "jjllmuse": "./src/cli.ts" },
  "scripts": {
    "build": "mkdir -p dist && bun build src/cli.ts --compile --target=bun-darwin-arm64 --outfile dist/jjllmuse-macos-arm64"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
